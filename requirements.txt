# LangGraph + Anthropic
langchain-anthropic>=0.3.21
langgraph>=0.4.0
langgraph-prebuilt>=0.2.0

# Code quality (used by hooks)
ruff>=0.9.0
black>=25.1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.25.0

# Async HTTP (for market API fetchers)
aiohttp>=3.11.0
